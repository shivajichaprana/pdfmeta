# input_pdf

Put the **PDF files** you want to tag in this folder.

Then run `pdfmeta run` from the project root. Each PDF here gets its metadata
applied and an edited copy is written to `../output/`. The originals in this
folder are never modified.
